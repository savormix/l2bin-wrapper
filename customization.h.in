#ifndef L2BIN_WRAPPER_CUSTOMIZATION_H

#define REPLACEMENT_EXECUTABLE L"@FILENAME_TO_REPLACE@"
#define TARGET_EXECUTABLE L"@FILENAME_TO_LAUNCH@"
#define TARGET_IP L"@ADDRESS_OVERRIDE_IP@"
#define TARGET_L2_INI L"@FILENAME_OVERRIDE_L2INI@"
#define TARGET_USER_INI L"@FILENAME_OVERRIDE_USERINI@"

#if (@OVERRIDE_IP@ != 0 || @OVERRIDE_L2INI@ != 0)
#define DISCARD_IP
#endif
#if (@OVERRIDE_IP@ != 0)
#define OVERRIDE_IP
#endif
#if (@OVERRIDE_L2INI@ != 0)
#define DISCARD_L2_INI
#define OVERRIDE_L2_INI
#endif
#if (@OVERRIDE_USERINI@ != 0)
#define DISCARD_USER_INI
#define OVERRIDE_USER_INI
#endif

#endif //L2BIN_WRAPPER_CUSTOMIZATION_H
